import { OwnerReviewMinimalDto } from './owner-review-minimal-dto';

describe('OwnerReviewMinimalDto', () => {
  it('should create an instance', () => {
    expect(new OwnerReviewMinimalDto()).toBeTruthy();
  });
});
